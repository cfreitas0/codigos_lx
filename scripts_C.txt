
           >>> INTRODUÇAO A LINGUAGEM C <<<

           >> 01 PASSAGEM DE ARGUMENTOS <<
#include <stdio.h>

int main(){
        char alvo[] = "google.com";
        int porta = 90;
        printf("iniciando ataque no alvo >> %s", alvo);
        printf("na porta >>> %d", porta);
        return 0;
}


#include <stdio.h>

int main () {

        char alvo[100];
        int porta;

        printf("Digite o nome do alvo:\n");
        scanf("%s", &alvo);

        printf("Digite o nome da porta:\n");
        scanf("%i", &porta);
        printf(" >>> iniciando atak no -> %s, na porta -> %i", alvo, port>
        return 0;
}

            >> 02 LACOS DE REPETICAO <<

#include <stdio.h>

int main (){
        int contador = 0;

        while(contador <= 10){
        printf("%i\n", contador);
        contador++;
}
        return 0;
}
